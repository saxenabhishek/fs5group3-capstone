<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fidelity.integration.ClientRegistrationMapper">
<resultMap type="Person" id="PersonTable">
<id property="id"				    column="ID" />
<result property="country"			column="COUNTRY" />
<result property="postalCode"		column="POSTALCODE" />
<result property="dob"		        column="DOB" />
<result property="email"		    column="EMAIL" />
<result property="password"		    column="PASSWORD" />
</resultMap>
<insert id="insertPerson" parameterType="com.fidelity.business.Person">
        INSERT INTO FT_PERSON (ID,COUNTRY, POSTALCODE, DOB, EMAIL, PASSWORD)
        VALUES (
            #{id},
            #{country},
            #{postalCode},
            #{dob},
            #{email},
            #{password}
        )
</insert>
<insert id="insertClientIdentification" parameterType="com.fidelity.business.ClientIdentification">
    INSERT INTO FT_CLIENT_IDENTIFICATION (ID, PERSON_ID, IDENTIFICATION_TYPE, IDENTIFICATION_VALUE)
    VALUES (#{id}, #{person_id}, #{type}, #{value})
  </insert>
  <insert id="insertClient" parameterType="com.fidelity.business.Client">
    INSERT INTO FT_CLIENT(ID,IDENTIFICATION_ID)
    VALUES (#{arg0}, #{arg1})
  </insert>
</mapper>


