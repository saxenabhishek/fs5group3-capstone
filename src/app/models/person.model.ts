export class Person {
  constructor(
    public id: string,
    public country: string,
    public postalCode: string,
    public dob: string,
    public email: string,
    public password: string
  ) {}
}
